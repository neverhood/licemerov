
<% allowed_items = eval(@photo.permissions) %>

<% if profile_owner? %>

    <div id='primary-rating-items'>
      <% current_user.rating_items[:primary].each do |item| %>
          <% if allowed_items[:primary].include?(item.to_sym) %>

              <div class='allowed-rating-item rating-item <%= item %>'>
                <label><%= t item %> <%= check_box_tag item, item, true, :class => 'hidden on-off-item' %></label>
              </div>

          <% else %>

              <div class='restricted-rating-item rating-item <%= item %>'>
                <%= t item %> <%= check_box_tag item, item, false, :class => 'hidden on-off-item' %>
              </div>


          <% end %>
      <% end %>

      <div id='rating-item-settings'>
        <a id='modify-photo-permissions'>Modify</a>
        <%= link_to 'Save', @photo, :method => :put, :remote => true, :id => 'save-photo-permissions', :class => 'hidden' %>
        <a id="cancel-photo-permissions-editing" class='hidden'>Cancel</a>
      </div>
    </div>

<% else %>

<% end %>