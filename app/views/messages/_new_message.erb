<%= form_for @message, :remote => true, :html => {:id => 'new-message'} do |f| %>

    <%= f.label :recipient %> <%= f.text_field :recipient %>

    <%= f.label :subject %> <%= f.text_field :subject %>
    <%= f.label :body %> <%= f.text_area :body %> <br>

<% end %>